---
description: Run the daily opener workflow.
argument-hint: [optional context]
---

Read `AGENTS.md`.
Read `.agents/skills/daily-opener/SKILL.md`.

Run Daily Opener.

Args / context:
$ARGUMENTS

Default: apply only to today's daily note if the path already exists; otherwise propose the path first.
Do not create other durable notes.
