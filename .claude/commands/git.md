---
description: Run a git safety check.
argument-hint: [optional scope]
---

Read `AGENTS.md`.
Read `.agents/skills/git-safety/SKILL.md`.

Run Git Safety Check.

Context:
$ARGUMENTS

Do not commit unless I explicitly ask.
