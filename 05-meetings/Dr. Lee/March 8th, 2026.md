Today's meeting was on effectively learning and evaluating prior representations for downstream tasks.

Key Ideas mentioned:
1. Look into **routing networks**, specifically [R. Yang, H. Xu, Y. Wu, and X. Wang. Multi-task reinforcement learning with soft modularization.]
2. Try to reconstruct into an early-stage latent space, instead of the exact original image. Why?
	1. Similar Idea: Instead of selecting in the patch-space, select in the token space.